### **1. 기본 설정 (Setup)**

가장 먼저 **로그-가능도 함수(Log-Likelihood Function)**, **스코어 함수(Score Function)**, **피셔 정보(Fisher Information)**를 구해야 합니다.

* **확률변수:** $X_i \stackrel{i.i.d}{\sim} N(\mu, \sigma^2)$ ($\sigma^2$는 이미 알고 있는 상수)
* **가설:** $H_0: \mu = \mu_0 \quad \text{vs} \quad H_1: \mu \neq \mu_0$

#### **(1) 로그-가능도 함수 $l(\mu)$**
가능도 함수 $L(\mu)$는 결합 확률밀도함수와 같습니다.

$$L(\mu) = \prod_{i=1}^n \frac{1}{\sqrt{2\pi\sigma^2}} \exp\left(-\frac{(x_i - \mu)^2}{2\sigma^2}\right)$$

양변에 로그를 취하면:

$$l(\mu) = -\frac{n}{2}\log(2\pi\sigma^2) - \frac{1}{2\sigma^2}\sum_{i=1}^n (x_i - \mu)^2$$

#### **(2) 최대우도추정량 (MLE, $\hat{\mu}$)**
로그-가능도 함수를 $\mu$에 대해 미분하여 0이 되는 값을 찾습니다.

$$l'(\mu) = \frac{d}{d\mu} l(\mu) = -\frac{1}{2\sigma^2} \sum 2(x_i - \mu)(-1) = \frac{1}{\sigma^2} \sum (x_i - \mu) = \frac{n}{\sigma^2}(\bar{x} - \mu)$$

$$l'(\hat{\mu}) = 0 \implies \hat{\mu} = \bar{x}$$

#### **(3) 피셔 정보 $I(\mu)$**
한 번 더 미분(2계 도함수)합니다.

$$l''(\mu) = \frac{d}{d\mu} \left[ \frac{n}{\sigma^2}(\bar{x} - \mu) \right] = -\frac{n}{\sigma^2}$$

피셔 정보는 2계 도함수의 기댓값에 음수를 취한 것입니다.

$$I(\mu) = -E[l''(\mu)] = \frac{n}{\sigma^2}$$

($\mu$에 의존하지 않는 상수입니다.)

---

### **2. Wald Test Statistic**

Wald 검정은 **"MLE($\hat{\mu}$)가 귀무가설($\mu_0$)과 얼마나 멀리 떨어져 있는가?"**를 봅니다.

* **정의:** $W = (\hat{\mu} - \mu_0)^2 I(\hat{\mu})$
* **전개:**

$$W = (\bar{x} - \mu_0)^2 \cdot \frac{n}{\sigma^2} = \frac{n(\bar{x} - \mu_0)^2}{\sigma^2}$$

이것을 표준정규분포 형태($Z$)로 묶으면 다음과 같습니다.

$$W = \left( \frac{\bar{x} - \mu_0}{\sigma/\sqrt{n}} \right)^2$$

---

### **3. Score Test Statistic (Rao's Score Test)**

Score 검정은 **"귀무가설($\mu_0$)에서의 기울기(Score)가 0에 얼마나 가까운가?"**를 봅니다. (MLE를 구할 필요 없이 $H_0$ 하에서만 계산합니다.)

* **정의:** $S = \frac{[l'(\mu_0)]^2}{I(\mu_0)}$
* **전개:**
    스코어 함수 값 $l'(\mu_0) = \frac{n}{\sigma^2}(\bar{x} - \mu_0)$
    피셔 정보 값 $I(\mu_0) = \frac{n}{\sigma^2}$

$$S = \frac{\left[ \frac{n}{\sigma^2}(\bar{x} - \mu_0) \right]^2}{\frac{n}{\sigma^2}} = \frac{\frac{n^2}{\sigma^4}(\bar{x} - \mu_0)^2}{\frac{n}{\sigma^2}} = \frac{n}{\sigma^2}(\bar{x} - \mu_0)^2$$

역시 정리하면:

$$S = \left( \frac{\bar{x} - \mu_0}{\sigma/\sqrt{n}} \right)^2$$

---

### **4. Likelihood Ratio Test Statistic (LRT)**

LRT는 **"귀무가설 하의 가능도($L(\mu_0)$)와 전체 최대 가능도($L(\hat{\mu})$)의 비율"**을 봅니다.

* **정의:** $\lambda_{LR} = -2 [ l(\mu_0) - l(\hat{\mu}) ]$
* **전개:**

$$l(\hat{\mu}) - l(\mu_0) = \left[ -\frac{1}{2\sigma^2}\sum(x_i - \bar{x})^2 \right] - \left[ -\frac{1}{2\sigma^2}\sum(x_i - \mu_0)^2 \right]$$
    
상수항 $-\frac{n}{2}\log(2\pi\sigma^2)$은 서로 뺄셈하여 사라집니다. 식을 정리하면:

$$= \frac{1}{2\sigma^2} \left[ \sum(x_i - \mu_0)^2 - \sum(x_i - \bar{x})^2 \right]$$

여기서 제곱합 분해 공식 $\sum(x_i - \mu_0)^2 = \sum(x_i - \bar{x})^2 + n(\bar{x} - \mu_0)^2$를 사용합니다.

$$= \frac{1}{2\sigma^2} \left[ n(\bar{x} - \mu_0)^2 \right]$$

따라서 통계량은:

$$\lambda_{LR} = 2 \times \left[ \frac{n(\bar{x} - \mu_0)^2}{2\sigma^2} \right] = \frac{n(\bar{x} - \mu_0)^2}{\sigma^2}$$

최종 형태:

$$\lambda_{LR} = \left( \frac{\bar{x} - \mu_0}{\sigma/\sqrt{n}} \right)^2$$

---

### **5. 결론 (Conclusion)**

정규분포($N(\mu, \sigma^2)$)에서 분산 $\sigma^2$이 알려진 경우의 평균 $\mu$에 대한 검정 통계량은 세 가지 방법 모두 동일한 결과를 줍니다.

$$Wald = Score = LRT = \left( \frac{\bar{x} - \mu_0}{\sigma / \sqrt{n}} \right)^2 \sim \chi^2_{(1)}$$

이것은 **Z-검정 통계량($Z$)의 제곱**과 같습니다.
