
---

### (1) If $X \perp Y | Z$, it is collapsible. $\rightarrow$ **False**

**해석:** "$Z$가 주어졌을 때 $X$와 $Y$가 조건부 독립이라면, ($Z$를 무시하고 합쳤을 때도) $X$와 $Y$는 독립(Collapsible)이다." 라는 주장입니다. 이는 **거짓**이며, 그 유명한 **심슨의 역설(Simpson's Paradox)**이 발생하는 상황입니다.

**증명 (Proof):**

1.  **조건부 독립(Conditional Independence)의 정의:**
    $$P(X=i, Y=j | Z=k) = P(X=i | Z=k) P(Y=j | Z=k)$$
    이를 확률 표기($\pi$)로 쓰면:
    $$\pi_{ijk} = \frac{\pi_{i+k} \pi_{+jk}}{\pi_{++k}}$$
    이때 조건부 오즈비는 모든 $k$에 대해 **1**입니다 ($\theta_{XY(k)} = 1$).

2.  **주변 확률(Marginal Probability) 계산:**
    $Z$에 대해 합(Sum)을 구하여 주변 확률 $\pi_{ij+}$를 구합니다.
    $$\pi_{ij+} = \sum_{k} \pi_{ijk} = \sum_{k} \frac{\pi_{i+k} \pi_{+jk}}{\pi_{++k}}$$

3.  **축약 가능성 확인:**
    만약 Collapsible하다면, 주변 테이블에서도 $X$와 $Y$가 독립이어야 하므로 $\pi_{ij+} = \pi_{i++}\pi_{+j+}$가 성립해야 합니다 (즉, 주변 오즈비 $\theta_{XY} = 1$).
    하지만, **위의 $\sum$ 식은 일반적으로 $\pi_{i++}\pi_{+j+}$와 같지 않습니다.**

    $$\sum_{k} \frac{\pi_{i+k} \pi_{+jk}}{\pi_{++k}} \neq \left(\sum_k \pi_{i+k}\right) \left(\sum_k \pi_{+jk}\right)$$

**결론:** $X$와 $Y$가 각 층($Z$) 내에서는 관련이 없더라도($\theta=1$), $Z$를 무시하고 합치면 가짜 상관관계(Spurious Correlation)가 생겨 $\theta \neq 1$이 될 수 있습니다. 따라서 Collapsible 하지 않습니다.

---

### (2) The homogeneous association, $(XY, XZ, YZ)$, is collapsible. $\rightarrow$ **False**

**해석:** 동질 연관 모형(모든 2-way interaction이 존재하는 모형)은 축약 가능한가?

**증명 (Proof):**

1.  **로그 선형 모형(Log-linear Model) 정의:**
    $(XY, XZ, YZ)$ 모형의 식은 다음과 같습니다.
    $$\log \mu_{ijk} = \lambda + \lambda_i^X + \lambda_j^Y + \lambda_k^Z + \lambda_{ij}^{XY} + \lambda_{ik}^{XZ} + \lambda_{jk}^{YZ}$$
    * $\lambda_{XYZ} = 0$ (동질 연관, 3차 항 없음)
    * $\lambda_{ik}^{XZ} \neq 0$ ($X$와 $Z$가 연관됨)
    * $\lambda_{jk}^{YZ} \neq 0$ ($Y$와 $Z$가 연관됨)

2.  **Collapsibility Theorem (축약 가능성 정리):**
    3차원 테이블에서 $Z$에 대해 축약했을 때 $X-Y$의 연관성 파라미터가 보존되려면, **다음 중 적어도 하나**는 성립해야 합니다.
    * $X \perp Z | Y$ (즉, $\lambda_{XZ} = 0$)
    * $Y \perp Z | X$ (즉, $\lambda_{YZ} = 0$)

3.  **모형 대입:**
    제시된 $(XY, XZ, YZ)$ 모형은 $XZ$항과 $YZ$항이 **모두 존재**합니다.
    즉, $Z$는 $X$와도 관련이 있고($\lambda_{XZ} \neq 0$), $Y$와도 관련이 있는($\lambda_{YZ} \neq 0$) **교란 변수(Confounder)** 역할을 합니다.

4.  **오즈비 비교:**
    * 조건부 오즈비(Conditional OR): $\theta_{XY(k)} = \exp(4\lambda_{11}^{XY})$ (모든 층에서 일정)
    * 주변 오즈비(Marginal OR): $\theta_{XY}$
    * 위의 정리(Theorem)에 의해 두 조건($\lambda_{XZ}=0$ or $\lambda_{YZ}=0$)을 모두 만족하지 못하므로:
        $$\theta_{XY(k)} \neq \theta_{XY}$$

**결론:** $Z$가 $X$와 $Y$ 모두에 영향을 미치고 있다면, $Z$를 무시하고 데이터를 합쳤을 때 $X$와 $Y$의 순수한 관계(오즈비)는 왜곡됩니다. 따라서 Collapsible 하지 않습니다.

---

### 요약 (Summary Table)

| 모형 (Model) | 조건부 독립 관계 | Collapsibility (over $Z$) | 이유 |
| :--- | :--- | :--- | :--- |
| **(1) $X \perp Y \vert Z$** | $(XZ, YZ)$ | **No** | $Z$가 $X, Y$ 모두와 연관되면 주변 독립이 깨짐 (심슨의 역설) |
| **(2) $(XY, XZ, YZ)$** | No Independence | **No** | $Z$가 교란변수($X, Y$ 모두와 연관)이므로 오즈비가 보존되지 않음 |
| **참고: $(XY, XZ)$** | $Y \perp Z \vert X$ | **Yes** | $Y$와 $Z$가 무관하므로 합쳐도 $XY$ 관계 보존됨 |
